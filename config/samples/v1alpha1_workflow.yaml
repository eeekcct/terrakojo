# Reference-only sample:
# In normal operation Workflow objects are created by BranchReconciler.
apiVersion: terrakojo.io/v1alpha1
kind: Workflow
metadata:
  labels:
    app.kubernetes.io/name: terrakojo
    app.kubernetes.io/managed-by: kustomize
  name: workflow-sample
spec:
  owner: "eeekcct"
  repository: "terrakojo"
  # Branch must reference Branch.metadata.name (resource name), not Git ref.
  branch: "branch-sample"
  sha: "0123456789abcdef0123456789abcdef01234567"
  template: "workflowtemplate-sample"
  path: "infra"
